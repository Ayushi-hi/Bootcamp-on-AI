import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifiesler
from sklearn.metrics import accuracy_scaore, confusion_matrix,classification_report

data = np.array([])